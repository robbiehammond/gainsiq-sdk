// jest.config.js
module.exports = {
    preset: 'ts-jest',
    testEnvironment: 'node',
    // Optionally, you can define which files to include as tests:
    testMatch: ['**/?(*.)+(test).[jt]s?(x)'],
  };